# 50-3 insert bulk data many data to the database

## insertMany() method of collection is used to upload many document/object in mongodb

### EXAMPLES
### client.db('ema-john-store').collection('products').insertMany([{}, {}, {}]).then(result => {}).catch(error => {});